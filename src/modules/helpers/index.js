const { formatResponse } = require("./format-response");
const {
    getCurrentTimeInMilliseconds,
} = require("./get-current-time-in-milliseconds");
const { getElapsedTime } = require("./get-elapsed-time");

module.exports = {
    formatResponse,
    getCurrentTimeInMilliseconds,
    getElapsedTime,
};
